import { SymbolsValidatorDirective } from './symbols-validator.directive';

describe('SymbolsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SymbolsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
